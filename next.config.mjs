/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/CompassQA',
    images: {
        unoptimized: true,
    },
    reactStrictMode: true,
}

export default nextConfig
